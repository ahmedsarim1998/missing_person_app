# Deployment requirements — pinned for the Docker image (Python 3.10).
#
# Trimmed, conflict-free subset of requirements.txt for cloud builds. Drops the
# OPTIONAL social-media analyzer stack (spacy / facebook-scraper / rapidfuzz),
# which the app imports defensively and runs fine without.
#
# Version notes (why these exact pins):
#  * TensorFlow 2.11 requires numpy<1.24 and protobuf<3.20.
#  * mtcnn pulls in the full `opencv-python` (not headless); recent opencv
#    (>=4.10) requires numpy>=2, which conflicts with TF. opencv 4.8.1.78 only
#    needs numpy>=1.21.2, so it coexists with numpy 1.23.5.
#  * scipy 1.9.3 is numpy-1.23 compatible.
# All pins verified to have cp310/abi3 manylinux wheels.

# --- Web framework / API ---
flask==2.2.5
flask-sqlalchemy==3.0.5
flask-cors==5.0.0
flask-jwt-extended==4.6.0
werkzeug==2.2.3
python-dotenv==0.21.1

# --- Production server ---
waitress==2.1.2

# --- Computer vision / AI engine ---
numpy==1.23.5
tensorflow==2.11.0
protobuf==3.19.6
opencv-python==4.8.1.78
keras-facenet==0.3.2
mtcnn==0.1.1
pillow
scipy==1.9.3

# --- Social-media analyzer (live Facebook group scanning) ---
# rapidfuzz has no numpy/TF dependency; facebook-scraper pulls only web-scraping
# libs (requests/lxml/dateparser), so neither conflicts with the CV stack above.
# lxml_html_clean: lxml>=5.2 moved lxml.html.clean into this separate package;
# requests-html (via facebook-scraper) still imports it, so install it explicitly.
rapidfuzz==3.9.7
facebook-scraper==0.2.59
lxml_html_clean==0.4.1
